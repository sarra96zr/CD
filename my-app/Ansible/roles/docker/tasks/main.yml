---
# tasks file for docker
- name: create image docker
  docker_image:
    name: First_app
    build:
     path: /var/lib/jenkins/workspace/First_app
     pull: no
    source: build
    state: present
    force_source: true
- name: run container docker
  docker_container:
    name: myapp_container
    image: First_app:latest
    state: started
    recreate: true
    ports:
     - "4200:80"
